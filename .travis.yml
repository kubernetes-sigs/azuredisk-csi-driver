language: go

go_import_path: sigs.k8s.io/azuredisk-csi-driver

services: docker

os: windows

before_script:
  - docker version

script:
  - |-
    docker login -u $PROW_AAD_CLIENT_ID -p $PROW_AAD_CLIENT_SECRET $REGISTRY
    make azuredisk-container-windows push
    docker logout
